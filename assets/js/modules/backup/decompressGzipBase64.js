/**
 * Módulo: Compactação removida
 * Responsabilidade: Mantido apenas por compatibilidade (não utilizado).
 */

export async function decompressGzipBase64ToText() {
	throw new Error('Compactação removida: backups compactados não são suportados.');
}
