/**
 * Módulo: Compactação removida
 * Responsabilidade: Mantido apenas por compatibilidade de import/export antigos.
 *
 * Importante:
 * - A compactação foi removida do app por solicitação.
 * - Esta função não deve mais ser usada.
 */

export async function compressGzipTextToBase64() {
	throw new Error('Compactação removida: exporte o backup em JSON normal.');
}
